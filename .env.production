# Production backend URL (used by `vite build`, mode=production).
# Must include the /api suffix. On Vercel prefer setting VITE_API_URL
# in Project Settings -> Environment Variables (Production) — it overrides this file.
VITE_API_URL=https://mini-app-backend-three.vercel.app/api